const initialState = {
  road: 0,
  trek: 0,
  pic: 0,
  rapid: 0
}

function score (state = initialState, action) {
  switch (action.type) {
    default: {
      return initialState
    }
  }
}

export default score
